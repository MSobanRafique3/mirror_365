// Placeholder widget test — update when features are built.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('app smoke test', (WidgetTester tester) async {
    // Full integration tests will be added as features are built.
    expect(true, isTrue);
  });
}
